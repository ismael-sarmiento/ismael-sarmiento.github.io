# ismael-sarmiento.github.io TODO

Use this file for deferred work that needs context or follow-up beyond inline code comments.

## Current Follow-Up

- Verify that `Cloudflare Web Analytics` starts receiving data after deployment.
- Publish the first bilingual article using `articles/_template/`.
- Expose the article listing in site navigation only after there is at least one real published article.
- Revisit `sitemap.xml` whenever new public pages are added.
- Validate mobile layout again after the first article pages are live.
